<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Only so the heartbeat job (JobScheduler, persisted) survives a
         reboot. Nothing in the app starts on boot any more. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Sideload-only single build (plan section 7.1), so the permissions
         Play restricts (REQUEST_INSTALL_PACKAGES for self-update,
         MANAGE_EXTERNAL_STORAGE for sharing the phone's storage) stay.
         Cleartext stays allowed app-wide: LAN servers are usually plain
         http by IP (plan WP0-6); https servers are used as https. -->
    <!-- allowBackup off: the session lives in Keystore-backed secure
         storage, which a backup restored to another phone can't decrypt. -->
    <application
        android:label="NivaroOS"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:allowBackup">
        <!-- Builds up to 1.2.x ran an always-on dataSync service through
             flutter_background_service. Android 15 limits dataSync to 6 h a
             day and forbids starting it at boot, and that plugin exports its
             service and receivers, restarts itself from an alarm and starts
             from BOOT_COMPLETED. Its components are removed here (the Dart
             package stays in pubspec for now but is not used), replaced by
             the app's own, all private to the app (plan M-18, M-19, M-33). -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            tools:node="remove" />
        <receiver
            android:name="id.flutter.flutter_background_service.WatchdogReceiver"
            tools:node="remove" />
        <receiver
            android:name="id.flutter.flutter_background_service.BootReceiver"
            tools:node="remove" />

        <!-- "Share this phone's storage": a user-started session with an end
             time, handling Android 15's dataSync timeout. -->
        <service
            android:name=".CompanionShareService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- The 15-minute heartbeat. JobScheduler binds it from the system
             process; BIND_JOB_SERVICE is a system-only permission, so no app
             can reach it. -->
        <service
            android:name=".HeartbeatJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <!-- Result of a self-update install session. -->
        <receiver
            android:name=".AppUpdateInstaller$InstallStatusReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
