# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com
project_name: nivaroos-app-management
before:
  hooks:
    - go generate
    - go run github.com/google/go-licenses@latest check . --disallowed_types=restricted
    - go mod tidy
    - go test -race -v ./... -timeout 30m
builds:
  - id: nivaroos-app-management-amd64
    binary: build/sysroot/usr/bin/nivaroos-app-management
    hooks:
      post:
        - cmd: go build -v -tags 'musl netgo osusergo' -o build/sysroot/usr/bin/appfile2compose github.com/F-e-n-y-x/NivaroOS/services/app-management/cmd/appfile2compose
          output: true
        - cmd: upx --best --lzma -v --no-progress build/sysroot/usr/bin/appfile2compose
          output: true
        - cmd: upx --best --lzma -v --no-progress "{{ .Path }}"
          output: true
    env:
      - CC=x86_64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - amd64
  - id: nivaroos-app-management-arm64
    binary: build/sysroot/usr/bin/nivaroos-app-management
    hooks:
      post:
        - cmd: go build -v -tags 'musl netgo osusergo' -o build/sysroot/usr/bin/appfile2compose github.com/F-e-n-y-x/NivaroOS/services/app-management/cmd/appfile2compose
          output: true
        # - cmd: upx --best --lzma -v --no-progress build/sysroot/usr/bin/appfile2compose
        #   output: true
        # - cmd: upx --best --lzma -v --no-progress "{{ .Path }}"
        #   output: true
    env:
      - CC=aarch64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm64
  - id: nivaroos-app-management-arm-7
    binary: build/sysroot/usr/bin/nivaroos-app-management
    hooks:
      post:
        - cmd: go build -v -tags 'musl netgo osusergo' -o build/sysroot/usr/bin/appfile2compose github.com/F-e-n-y-x/NivaroOS/services/app-management/cmd/appfile2compose
          output: true
        - cmd: upx --best --lzma -v --no-progress build/sysroot/usr/bin/appfile2compose
          output: true
        - cmd: upx --best --lzma -v --no-progress "{{ .Path }}"
          output: true
    env:
      - CC=arm-linux-gnueabihf-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"
  - id: nivaroos-app-management-migration-tool-amd64
    binary: build/sysroot/usr/bin/nivaroos-app-management-migration-tool
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=x86_64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - amd64
  - id: nivaroos-app-management-migration-tool-arm64
    binary: build/sysroot/usr/bin/nivaroos-app-management-migration-tool
    # hooks:
    #   post:
    #     - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=aarch64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm64
  - id: nivaroos-app-management-migration-tool-arm-7
    binary: build/sysroot/usr/bin/nivaroos-app-management-migration-tool
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=arm-linux-gnueabihf-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"
  - id: nivaroos-app-management-validator-amd64
    binary: build/sysroot/usr/bin/nivaroos-app-management-validator
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/validator
    env:
      - CC=x86_64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - amd64

archives:
  - name_template: >-
      {{ .Os }}-{{- if eq .Arch "arm" }}arm-7{{- else }}{{ .Arch }}{{- end }}-{{ .ProjectName }}-v{{ .Version }}
    id: nivaroos-app-management
    builds:
      - nivaroos-app-management-amd64
      - nivaroos-app-management-arm64
      - nivaroos-app-management-arm-7
    files:
      - build/**/*
  - name_template: >-
      {{ .Os }}-{{- if eq .Arch "arm" }}arm-7{{- else }}{{ .Arch }}{{- end }}-{{ .ProjectName }}-migration-tool-v{{ .Version }}
    id: nivaroos-app-management-migration-tool
    builds:
      - nivaroos-app-management-migration-tool-amd64
      - nivaroos-app-management-migration-tool-arm64
      - nivaroos-app-management-migration-tool-arm-7
    files:
      - build/sysroot/etc/**/*
  - name_template: >-
      {{ .Os }}-{{- if eq .Arch "arm" }}arm-7{{- else }}{{ .Arch }}{{- end }}-{{ .ProjectName }}-validator-v{{ .Version }}
    id: nivaroos-app-management-validator
    builds:
      - nivaroos-app-management-validator-amd64
    files:
      - build/sysroot/etc/**/*

checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "{{ incpatch .Version }}"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
release:
  github:
    owner: F-e-n-y-x
    name: nivaroos
  draft: true
  prerelease: auto
  mode: replace
  name_template: "v{{ .Version }}"
