// go:generate regeneration disabled after forking into Recasa: the previous
// directive pulled the OpenAPI spec from the original upstream project's
// GitHub org. The generated output (codegen/mod_management/api.go) is
// already vendored here; point this at a Recasa-owned spec source before
// re-enabling regeneration.
package interfaces
